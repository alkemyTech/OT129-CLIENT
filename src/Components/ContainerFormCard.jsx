import React from 'react';

const ContainerFormCard = ({ children }) => {
    return <div className='w-50 m-auto border border-light shadow-lg bg-body rounded'>{children}</div>;
};

export default ContainerFormCard;
